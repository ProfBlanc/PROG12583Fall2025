# Task 1
- Create a function named get_text_input()
    - ask user for a text input
    - allow user to customize the prompt text
    - enforce that answer must be at least 3 chars
        - if not, answer to default to "abc"
        - if yes, accept the user answer
    - return the final answer